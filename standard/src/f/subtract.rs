/// Subtracts its second argument from its first argument
///
/// # Examples
///
/// ```
/// use standard::f;
///
/// let result = f::subtract(3, 2);
/// assert_eq!(result, 1);
/// ```
pub fn subtract (x: i32, y: i32) -> i32 {
    x - y
}
