/// Multiplies two numbers
///
/// # Examples
///
/// ```
/// use standard::f;
///
/// let result = f::multiply(3, 2);
/// assert_eq!(result, 6);
/// ```
pub fn multiply(x: i32, y: i32) -> i32 {
    x * y
}
